spring:
  datasource:
    url: jdbc:h2:mem:neighbor_parking;MODE=MySQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver

app:
  auth:
    dev-enabled: true
  jwt:
    # 仅用于本地演示，生产配置不得复用。
    secret: local-demo-secret-local-demo-secret-2026
